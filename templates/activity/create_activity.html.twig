{% extends 'base.html.twig' %}

{% form_theme formActivity 'bootstrap_4_layout.html.twig' %}

{% block body %}

<div class="container-fluid">
    <div class="row">
        <div class="col-3"></div>
        <div class="col-6"> {{ form_start(formActivity)}}
                {{ form_row(formActivity.title, { 'label': 'Titre de l\'activité' }) }}
                {{ form_row(formActivity.description, { 'label': 'Description' })  }}
                {{ form_row(formActivity.note, { 'label': 'Note', 'attr': {'class': 'foo'} } )  }}
                {{ form_row(formActivity.category, { 'label': 'Choisissez une categorie' })  }}

                <ul class="pictures" data-prototype="{{ form_widget(formActivity.pictures.vars.prototype)|e('html_attr') }}">
                      
                    </ul>
                {{ form_end(formActivity)}}
        </div>
        <div class="col-2"></div>

    </div>
</div>

 
{% endblock %}

{% block javascripts %}
<script type="text/javascript" src="https://ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js"></script>
{{ encore_entry_script_tags('app') }}
{{ encore_entry_script_tags('pictureScript') }}
{% endblock %}
   

