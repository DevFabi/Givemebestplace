{% extends 'base.html.twig' %}

{% block body %}

<div class="container">
    <div class="row">
      <div class="col-md-3"></div>
      <div class="col-md-6">
        {% for category in categories %}
        
        <a class="nav-item" href="{{path ('show_category', {'id' : category.id })}}">{{ category.title }}</a>
      
        
        {% endfor %}
      </div>
      </div>
   </div>
    <div class="row">
            {% for activity in activities %}
            <div class="col-md-4">  
              <div class="fond-article">           
          <h3> <a href="{{path ('show_activity', {'id' : activity.id })}}">{{ activity.title }}</a> </h3> 
           <p> {{ activity.description }}</p>
        <p> Note :  {{ activity.note }}</p>
          <p>Dans la catégorie  {{ activity.category.title }}</p>
        </div> 
        </div>
        {% endfor %}
        
       
    </div>
</div>


{% endblock %}